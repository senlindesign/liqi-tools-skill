# 利器工具资料库

## 目标

把利器公开访谈转成可追溯的创作者工具资料库，并最终封装成按任务、场景和限制条件检索的 Skill。

## 数据边界

- 以 `https://liqi.io/categories/interview/` 为访谈主索引。
- 收录具体人物或团队为主体的问答式访谈，包括普通访谈、VLOG、播客和通信者系列。
- 暂不把周报、城堡阅读周刊、编辑合集作为“创作者推荐”。它们保留在 manifest 中，标记为排除，供后续扩展。
- 原始访谈层不把编辑说明误标为受访者观点。
- 工具提及必须保留访谈 ID、原始 URL 和足以核对的证据位置。

## 许可与使用

利器页面声明内容按 CC-BY-NC-SA 分享。当前站点 robots 内容信号允许搜索和 reference、禁止 AI 训练。本项目据此：

- 保留来源、原链接和许可证；
- 不把资料用于模型训练或微调；
- Skill 默认返回归纳、短证据和原文链接，不返回整篇访谈；
- 对外发布时采用兼容的署名、非商业、相同方式共享条款。

## 数据层

1. `data/interviews-manifest.jsonl`：站点访谈索引与收录判定。
2. `interviews/`：用于校对和追溯的访谈 Markdown。
3. `data/tool-candidates.jsonl` 与 `data/unlinked-candidates.jsonl`：每一次“创作者在某个场景提到某工具”的机器提取候选；打包后的 SQLite `mentions` 表是统一检索入口。
4. `data/entities.provisional.jsonl`：机器抽取的规范化实体和别名（尚未全部人工确认）。
5. `data/review-queue.jsonl`：按出现频率和证据类型排序的人工复核队列。
6. `data/creator-profiles.jsonl`：创作者档案中间层。
7. `data/workflow-cases.jsonl`：按创作者和工作阶段组织的案例层。
8. `data/tool-aggregates.jsonl`：跨创作者工具聚合层。

机器抽取结果必须经过结构校验；工具的当前可用性属于另一项需要联网验证的事实，不从历史访谈自动推定。

## 发布执行清单

### 已完成

- [x] 抓取访谈索引并判定收录范围
- [x] 保存 251 篇完整访谈 Markdown
- [x] 提取工具相关段落、链接候选和暂定实体
- [x] 建立 SQLite 检索库和本地搜索脚本
- [x] 完成两批、40 条工具记录的人工校准
- [x] 建立 reviewed / provisional 双层数据模型
- [x] 建立创作者档案、Workflow / Case、跨创作者工具聚合三层
- [x] 增加 task / workflow / tool 三种检索入口
- [x] 打包 Codex/Claude 共用的 `SKILL.md`
- [x] 通过数据验证、Skill 验证和发布候选验收
- [x] 统一 Task / Workflow / Tool 三种入口的 onboarding 与回答状态
- [x] 增加中文意图归一化、约束分离和局部证据窗口排序
- [x] 为工具聚合补充“创作者—使用场景—证据—来源”映射
- [x] 增加无结果、负例、排序、体积、耗时与触发边界评测
- [x] 精简安装包中的重复维护数据
- [x] 拆分 MIT 代码许可与访谈语料许可说明
- [x] 增加 GitHub Actions 发布门禁

### 每次发布必须保持

- [x] GitHub 仓库公开，许可边界单独说明
- [x] `make release-check` 重建、验证并执行回归评测
- [x] CI 在 push 与 pull request 上运行同一发布门禁
- [x] 用视频剪辑、播客、写作、密码管理、Notion 聚合和无结果查询回归

### 持续迭代

- [ ] 按 `data/review-queue.jsonl` 继续分批校准实体
- [ ] 为高频工具补充别名、平台限制、价格/可用性验证日期
- [ ] 将用户反馈转成新的检索词、场景标签和评测样例
- [ ] 站点内容更新时重新生成 manifest、Markdown 和数据库

发布原则：当前版本可以发布为“来源可追溯的 provisional corpus + 40 条 reviewed records”。后续校准是增量更新，不阻塞首个 GitHub 版本，但任何未校准记录都必须继续显示不确定性。

## v1 发布约定

### 对用户承诺

- 按任务提供少量、来源可追溯的工具建议；
- 按 Workflow / Case 展示创作者如何完成相似工作；
- 按工具聚合不同创作者的具体用法与已校准评价；
- 每项结论都附原始访谈链接，并标注 `已核对访谈` 或 `访谈线索`。

### 不作出的承诺

- 不把历史访谈中的工具、价格、版本、隐私或可用性表述为当前事实；
- 不把工具出现次数当成推荐强度；
- 不声称所有暂定实体都经过人工编辑；
- 不复制整篇访谈或长段原文。

### 状态定义

- `reviewed`：人工核对过创作者、场景、来源与推荐强度。
- `provisional`：机器抽取的候选，仅用于发现；对外回答必须说明不确定性。
- `machine_case`：按访谈段落生成的案例线索，不等同于完整工作流。
- `reviewed_case`：人工整理的“任务—步骤—工具—原则—限制”案例。

### v1 完成门槛

- 三个入口均有可验证输出，并优先使用已校准内容；
- 至少覆盖写作、视频、播客、产品/设计、开发五类完整人工校准案例；
- 所有发布包数据可由脚本重建，并通过验证、评测和 Skill 格式检查；
- GitHub 文档完整说明安装、许可、数据状态和再生成方式。
